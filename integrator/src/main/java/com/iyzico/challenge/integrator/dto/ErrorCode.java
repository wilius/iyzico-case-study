package com.iyzico.challenge.integrator.dto;

public enum ErrorCode {
    NOT_AUTHORIZED,
    API_NOT_FOUND,
    INVALID_CREDENTIALS,
    INVALID_REQUEST,

    USER_NOT_FOUND,

    UNEXPECTED_ERROR
}
